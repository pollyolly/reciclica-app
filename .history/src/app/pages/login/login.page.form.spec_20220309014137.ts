import { LoginPageForm } from "./login.page.form";

describe('LoginPageForm',()=>{
    it('should create login form empty', ()=>{
        const loingPageForm = new LoginPageForm();
        
        expect(false).toBeTruthy(); //Intentional Error To test
    })
})